% Johann Diep (jdiep@student.ethz.ch) - June 2019

% This function reads the position ground-truth data from the Vicon system
% over a ROS bridge.

function drone_position_gt = getGroundTruth()
    %% ROS communication initialization
    
    rosinit;
    
    %% Reading position data and plotting
    
    rostopic = ['/vicon/Johann_Bebop/Johann_Bebop'];
    ros_obj.ViconPos_sub = rossubscriber(
    
    
    %% Closing ROS communication
    
    rosshutdown;
end